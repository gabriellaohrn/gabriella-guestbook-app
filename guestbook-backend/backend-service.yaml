apiVersion: v1
kind: Service
metadata:
  name: gabriella-python-service
  namespace: grupp5
  labels:
    app: gabriella-python-service
spec:
  selector:
    app: gabriella-python-service
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
